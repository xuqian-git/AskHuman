# PROGRESS

按具体任务 / 需求记录待办与当前进展。任务 / 需求完成后删除其 section（历史留在 git）。

## 推荐选项显式声明（`-o!`）

代码已实现、单测通过（spec/plan 见 `docs/specs/recommended-option.md` / `docs/plans/recommended-option.md`）。剩余：按用户要求**暂不跑 install.sh**，待用户发起后用新版二进制实测（弹窗 Badge / 提交原文 / IM 渠道前缀），实测通过后删除本节。
